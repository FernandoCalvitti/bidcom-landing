export interface GetStartedResource {
  headline: string;
  title: string;
  description: string;
  button: GetStartedButton;
}

export interface GetStartedButton {
  title: string;
  link: string;
}
